= content_for :main_toolbar do
  = "Kubernetes as a Service (Gardener #{@service_name.capitalize})"

= javascript_include_tag "kubernetes_ng_app_widget", data: {basepath: plugin("kubernetes_ng").service_path(service_name: @service_name), mountpoint: Core::PluginsManager.plugin("kubernetes_ng").mount_point, region: current_region }
