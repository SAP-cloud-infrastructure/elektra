= content_for :main_toolbar do
  = "Kubernetes as a Service (Gardener #{@landscape_name.capitalize})"

= javascript_include_tag "kubernetes_ng_app_widget", data: {basepath: plugin("kubernetes_ng").service_path(landscape_name: @landscape_name), mountpoint: Core::PluginsManager.plugin("kubernetes_ng").mount_point, region: current_region }
