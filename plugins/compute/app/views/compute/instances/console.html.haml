= content_for :title do
  %i.fa.fa-terminal
  Web Console
- if modal?
  .modal-body.console
    %iframe{src: @console.url, width: '100%', height: '650px'}
- else
  - if @console_error
    .alert.alert-error
      #{@console_error}
  - elsif @console.nil?
    .alert.alert-warning
      The console is not available 🙄
  - else
    -# check the layout for in layouts/compute/console.html.haml
    -# where yield is used to render the console content
    -# the only content that gets yielded is what's outside the content_for and modal? blocks 
    -# which is just the iframe
    %iframe{src: @console.url, width: '100%', height: '850px'}

- if modal?
  .modal-footer
    - if modal?
      %button.btn.btn-default{type:"button", data: {dismiss:"modal"}, aria: {label: "Close"}} Close
    - else
      = link_to "Close", plugin('compute').instances_url, class: 'btn btn-default'
