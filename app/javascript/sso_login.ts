// Entry point for the browser-side SSO login module.
// This file exists at the top level of app/javascript/ so that esbuild
// picks it up as a standalone bundle (output: app/assets/builds/sso_login.js).
import "core/sso_login"
